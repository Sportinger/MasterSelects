{
  "name": "masterselects-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-features.mjs",
    "sync:head": "node scripts/sync-features.mjs --source-ref HEAD",
    "dev": "npm run sync && astro dev",
    "build": "npm run sync && astro build",
    "build:head": "npm run sync:head && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.36.0",
    "astro": "^5.0.0",
    "sharp": "^0.34.0"
  }
}
