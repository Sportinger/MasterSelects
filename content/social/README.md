# Local maintainer-tool defaults

These empty examples let the optional Admin Social Center and social CLI build
from the public source. No accounts, campaign drafts, or business metrics are
included. The dated post filename is retained because the admin source imports
it; its contents are an unused example draft with no publication schedule.

Maintainers configure their own accounts and content separately. Credentials
belong in environment variables, never in these files.
